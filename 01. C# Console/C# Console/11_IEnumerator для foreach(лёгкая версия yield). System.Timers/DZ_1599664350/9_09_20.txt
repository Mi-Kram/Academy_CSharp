1. Разработать класс MyDate, который принимает год и месяц и в цикле foreach возвращает дни
этого месяца.

Пример:

MyDate md = new MyDate(10, 1998);
foreach(int day in md)
{
	Console.WriteLine(day);
}

Пример вывода:
1
2
3
4
5
6
7
8
...

2. Программа сканирует по таймеру 1 раз в сек. определённую папку и её подпапки и удаляет все файлы, 
расширения которых заданы в файле config.txt

Пример config.txt:
.txt
.bmp
.jpg

3. Программа сканирует по таймеру 1 раз в сек. определённую папку и её подпапки и копирует
все картинки в папку BackUp. Файлы, которые были скопированы в прошлое срабатывание таймера
больше не копируются. Если в папке BackUp имена повторяются, то имя нового файла должно быть
изменено.